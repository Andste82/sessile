# jenkins

Lets your task agent check a job's last build, read the tail of a build
log, and (with your approval) start a build. Authenticates with a Jenkins
user and API token.

Adapt it freely: download the zip, change `main.py`, bump the version in
`meta.json` and upload it again. Your settings are kept.

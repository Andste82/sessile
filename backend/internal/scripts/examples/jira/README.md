# jira

Lets your task agent read and search Jira tickets, and (with your approval)
comment on and create them. Works with Jira Cloud (e-mail + API token) and
Jira Server / Data Center (personal access token).

Adapt it freely: download the zip, change `main.py`, bump the version in
`meta.json` and upload it again. Your settings are kept.

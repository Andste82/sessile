# artifactory

Lets your task agent find artifacts in JFrog Artifactory, read their
details and browse repository folders. Read-only. Authenticates with an
access or identity token.

Adapt it freely: download the zip, change `main.py`, bump the version in
`meta.json` and upload it again. Your settings are kept.
